let num = 1;

while (num <= 40) {
 if (num % 2 !== 0) { // could also use num % 2 === 1
   console.log(num);
 };
 num += 1; 
}

// could also not use the if statement and console.log(num) and then increment by 2